# Live acceptance

After source proof and protected integration, use the existing canonical #2158 bootstrap/sync machinery. The Battle Bridge must converge its checkout and physical Mission Worker/runtime truth to protected main, then the goal flywheel must autonomously produce one real durable-goal chain:

`SELECT -> CLAIM -> SOURCE_CHANGED -> TESTED -> terminal execution receipt -> exact-head review handoff`.

Anything shorter remains #1622 repair-in-progress.
