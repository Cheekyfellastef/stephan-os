# Test matrix

- observed cost model learns P50/P80;
- status work routes to zero-cost lane;
- capacity reserves remain protected;
- oversized work defers to natural reset;
- earliest-expiring reset is selected;
- imminent natural reset preserves banked reset;
- missing standing policy blocks automatic redemption;
- stack velocity uses verified slices only;
- Remote Codex skill is fixed-action and fail-closed;
- meter-aware dispatcher suppresses zero-cost work;
- meter-stalled dispatcher prepares reset instead of dispatching;
- sufficient capacity invokes dispatcher once;
- dashboard shows four resets, next expiry, route and velocity.
