# PR scope

This branch contains only additive Meter-Aware Builder Mesh V1 source, tests, documentation, a dedicated proof workflow, a read-only dashboard projection, and a bounded Remote Codex reset skill.

It does not mutate existing dispatcher, dashboard, Shared Workspace, capability registry, runtime, browser session, billing, or account settings.
