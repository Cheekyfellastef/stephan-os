# Live boundary

The source branch prepares decisions and bounded action contracts only.

A real reset redemption requires:

- an authenticated supported Codex browser surface;
- a fresh real meter observation;
- a live standing-policy record;
- the selected earliest-expiring reset still present;
- exact before/after proof;
- a Shared Workspace completion receipt.

Until then, the live verdict remains `LIVE_ACCEPTANCE_PENDING`.
