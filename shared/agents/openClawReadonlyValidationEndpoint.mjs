export const OPENCLAW_READONLY_VALIDATION_ENDPOINT = Object.freeze({
  available: true,
  path: '/api/openclaw/health-handshake/validate-readonly',
  mode: 'local_readonly_probe',
  canExecute: false,
});
