name: One-shot Music Auto Artwork Observer Fix

on:
  pull_request:
    branches: [main]
    types: [opened, synchronize, reopened]
    paths:
      - 'scripts/one-shot-music-auto-artwork-observer-fix.mjs'
      - '.github/workflows/one-shot-music-auto-artwork-observer-fix.yml'

permissions:
  contents: write
  pull-requests: read

concurrency:
  group: one-shot-music-auto-artwork-observer-fix-${{ github.event.pull_request.number }}
  cancel-in-progress: true

jobs:
  repair:
    if: github.event.pull_request.head.repo.full_name == github.repository && github.event.pull_request.head.ref == 'fix/music-tile-automatic-url-artwork-v2'
    runs-on: ubuntu-24.04
    timeout-minutes: 10
    steps:
      - name: Check out exact repair branch
        uses: actions/checkout@v4
        with:
          ref: fix/music-tile-automatic-url-artwork-v2
          fetch-depth: 0
          token: ${{ github.token }}

      - name: Apply bounded observer-loop repair
        run: node scripts/one-shot-music-auto-artwork-observer-fix.mjs

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install focused test dependencies
        run: npm install --ignore-scripts --no-audit --no-fund

      - name: Run source and Music Tile regressions
        shell: bash
        run: |
          set -euo pipefail
          node --check apps/music-tile/engine/nativeCatalogAutoApply.js
          node --test tests/music-tile-auto-url-artwork-source-contract.test.mjs
          node --test tests/music-intelligence-centre-vnext.test.mjs
          git diff --check

      - name: Publish settled repair and remove diagnostics
        shell: bash
        run: |
          set -euo pipefail
          git config user.name "Stephanos GitHub Repair"
          git config user.email "actions@users.noreply.github.com"
          git restore -- package-lock.json 2>/dev/null || true
          rm -f scripts/one-shot-music-auto-artwork-observer-fix.mjs
          rm -f .github/workflows/one-shot-music-auto-artwork-observer-fix.yml
          rm -f .github/workflows/one-shot-music-auto-artwork-focused-edge.yml
          rm -f .github/workflows/one-shot-music-auto-artwork-focused-chrome.yml
          git add \
            apps/music-tile/engine/nativeCatalogAutoApply.js \
            tests/music-tile-auto-url-artwork-source-contract.test.mjs \
            scripts/one-shot-music-auto-artwork-observer-fix.mjs \
            .github/workflows/one-shot-music-auto-artwork-observer-fix.yml \
            .github/workflows/one-shot-music-auto-artwork-focused-edge.yml \
            .github/workflows/one-shot-music-auto-artwork-focused-chrome.yml
          git diff --cached --check
          git commit -m "Stop Music Tile artwork hydration loops"
          git push origin HEAD:fix/music-tile-automatic-url-artwork-v2
